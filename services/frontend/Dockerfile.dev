# Development Dockerfile for React + TypeScript + Vite Frontend
FROM node:22-alpine

# Install curl for health checks
RUN apk add --no-cache curl

# Set working directory
WORKDIR /app

# Copy package files and install dependencies
# This will be overridden by volume mount, but needed for initial build
COPY package*.json ./
RUN npm ci

# Expose the default Vite dev server port
EXPOSE 5173

# Running as root for development (simpler setup)

# Default command to start the development server
# The source code will be mounted as a volume
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
